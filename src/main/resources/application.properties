# Server port (default:8080:
# because the Spring Boot framework provides the default embedded server (Tomcat) to run the Spring Boot application)
server.port=8080

# Data properties
spring.datasource.url=jdbc:mysql://localhost:3306/testthuctap
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate properties
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

###### Email Properties ######
# smtp mail properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=from9209@gmail.com
spring.mail.password=crqksmbisgvsmthw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
#spring.security.enabled=false
